document.addEventListener("DOMContentLoaded", function () {
    // Show view JS - mostly for visual enhancements if needed
    // Currently no interaction required for read-only view
    console.log("Docente details loaded");
});
