<?php

// Helpers específicos para ZoofiPets - Sistema Veterinario

if (!function_exists('format_mascota_info')) {
    function format_mascota_info($mascota) {
        // Helper para formatear información de mascotas
    }
}

if (!function_exists('calcular_edad_mascota')) {
    function calcular_edad_mascota($fecha_nacimiento) {
        // Helper para calcular edad de mascotas
    }
}

if (!function_exists('generar_codigo_cita')) {
    function generar_codigo_cita() {
        // Helper para generar códigos únicos de citas
    }
}